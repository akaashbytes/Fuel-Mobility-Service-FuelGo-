server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:oracle:thin:@localhost:1521/XEPDB1
    username: nexfuel
    password: password
    driver-class-name: oracle.jdbc.OracleDriver
  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

jwt:
  secret: "9a76a5b432e1f0987654321fedcba0987654321fedcba0987654321fedcba0987654321fedcba09"
  accessTokenExpirationMs: 900000 # 15 Minutes (15 * 60 * 1000)
  refreshTokenExpirationMs: 604800000 # 7 Days (7 * 24 * 60 * 60 * 1000)

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
